"""Wine resolution package (Stufe 2).

Version constants are bumped manually whenever a normalization, vintage, or
resolution rule changes. They are persisted in the database so that any change
is traceable and reproducible.
"""

LOADER_VERSION = "1.0.0"
NORMALIZER_VERSION = "1.0.0"
RESOLVER_VERSION = "1.0.0"
